这是一个面试辅助工具，我已经写了一个初版出来
现在我的计划是：
重构这个工具，在保留核心能力的同时增加新能力、新ui，具体要求为：

#要求实现的功能
1、不再使用py的ui界面，转为使用一个webui，前端需要在浏览器中去访问
2、客户端仅需要简易界面，支持登录注册，填入大致参数或者内容啊之类的简易ui，挂在后台运行服务即可
3、客户端程序，自动选择并监听扬声器（也可手动选择），然后自动运行服务。展示和结果在上述前端webui
4、支持注册和登录，增加云端服务去作为后台和收费（收费模块暂时出版不需要，后续在写，暂时先默认所有账号无限额度）（比如需要购买使用额度啊、次数啊之类的，商业化一下）但是计费、统计之类的需要有。比如ai使用情况、转语音时常之类的。仅仅只是账户的余额暂定为都是无限而已。以及，要支持简单的用户组。我是管理员由最高权限，永久无限额度；特级用户无限额度，但是没有管理权限，高级用户有特定折扣，普通用户正常计费，等功能。后续会改为需要付费的额度之类的。预留好，计划好。
5、webui最好加上一些简易的美观的东西，比如一些ui之类
6、需要支持当前的核心能力，客户端识别当前面试时候的系统声音，转文字，并让ai解答（尽量重构，并增加准确率和ai使用的方法啊之类的，比如tools或者mcp之类）
7、云端可以添加简历输入上传读取和解读能力，根据输入的岗位要求和描述，给出用户简历修改的建议（暂时不必实现，预留拓展的口子即可。暂时仅迁移老版已有的功能）
8、云端需要支持ai知识库系统，建议使用简易的向量数据库之类，云端向量数据也行，建立了账号用户体系之后在云端处理也行，每个用户的知识库是隔离的，你看着来（暂时不必实现，预留拓展的口子即可。暂时仅迁移老版已有的功能）
9、要求尽可能最大化使用ai的能力，并且可以监控并记录ai的使用量，便于后续收费，阿里云的语音识别的识别时长使用量也得记录，也是收费的
10、尽量模块化、可拓展化、需要你好好架构一下，功能比较多比较复杂
11、老版本已经被我备份在backup下了，你可以参考
12、注意必须实现防检测防录屏防切屏功能。客户端主要用户登录然后最小化，主要还是截图的时候需要做到防护。
13、尽量简单高效，避免过度设计，不要臃肿化
14、需要支持win、macos
15、云端服务需要支持一件docker或者docker compose可以run起来。主要模块可以拆分、扩容。（现在出版可以默认作为一个单例服务去跑，不必拆分。但是得留口子，以后可能会分成微服务）